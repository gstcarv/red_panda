# API Configuration
# Base URL for the backend API
VITE_API_BASE_URL=http://localhost:8080/api

# MSW (Mock Service Worker) Configuration
# Enable API mocking with MSW (set to "true" to enable, "false" to disable)
# Default: false
VITE_ENABLE_MSW=false
